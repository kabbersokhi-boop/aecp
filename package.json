{
  "name": "aecp-browser-validation",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "check": "node --check src/aecp/static/app.js && node --check scripts/browser_smoke.cjs && node --check scripts/provider_browser.cjs && node --check scripts/semantic_browser.cjs",
    "test:browser": "node scripts/browser_smoke.cjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
